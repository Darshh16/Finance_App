<resources>

    <!-- Base light theme -->
    <style name="Theme.PersonalFinanceApp"
        parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">#1A73E8</item>
        <item name="colorPrimaryVariant">#1557B0</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorSecondary">#03DAC5</item>
        <item name="colorSurface">@android:color/white</item>
        <item name="android:colorBackground">#F5F7FA</item>
        <item name="android:windowBackground">#F5F7FA</item>
    </style>

    <!-- No action bar variant (used for Splash) -->
    <style name="Theme.PersonalFinanceApp.NoActionBar"
        parent="Theme.PersonalFinanceApp">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

</resources>
